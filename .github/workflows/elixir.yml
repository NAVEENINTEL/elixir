name: Elixir CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Run Tests
    runs-on: self-hosted
    
    env:
      ImageOS: ubuntu20

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Elixir
        uses: actions/setup-elixir@v1
        with:
          otp-version: '24.x'

      - name: Install dependencies
        run: mix deps.get

      - name: Run tests
        run: mix test
